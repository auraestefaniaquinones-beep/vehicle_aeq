generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Vehicle {
  id        Int      @id @default(autoincrement())
  brand     String
  model     String
  plate     String   @unique
  color     String
  year      Int
  createdAt DateTime @default(now())
}